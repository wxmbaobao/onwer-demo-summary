1.介绍与安装
2.工作流：工作区——>暂存区——>版本库
3.初始化：git init
         git add . (工作区——>暂存区)
         git commit -m '......'(暂存区——>版本库)
4.远程仓库：git remote add .
           git pull
           git push
           git clone
5.分支管理：git branch (列出所有我们当前的分支)
           git chechout (切分支)
           git merge
6.标签管理：git tag
           git push
           
疑问？
回滚：正常操作我都是git push 后的
现在回滚，
先 git revert --hard 要回滚的版本号
在 git reset 最新的版本号
此时代码变成最新的版本号，可以修改代码在提交了
(不确定，我得在深究一下)